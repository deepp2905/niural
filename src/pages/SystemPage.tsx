import { Placeholder } from '../components/Placeholder';

export function SystemPage() {
  return (
    <Placeholder title="Design system reference" phase="Phase 7 · §8f">
      Token swatches (both themes), type scale, Input state-matrix grid, StatusChip set, and a
      SettlementLedger in both emphasis modes.
    </Placeholder>
  );
}
